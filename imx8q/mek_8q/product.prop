# This file is included from core config.mk, do not include it in local makefiles
# Set supported Bluetooth profiles to enabled (See Car service bluetooth.prop for base BT profiles)
bluetooth.profile.hid.device.enabled?=true
bluetooth.profile.hid.host.enabled?=true
