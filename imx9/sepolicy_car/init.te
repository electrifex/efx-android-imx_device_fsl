recovery_only(`
  # Files in recovery image are labeled as rootfs.
  domain_trans(init, rootfs, evs_service_recovery)
  domain_trans(init, rootfs, display_modules_recovery)
')

allow init sysfs_canregister:file { open write };
allow init rootfs:system { module_load };
allow init self:capability { sys_module };
